# Only needed in Mode B (when the host has no native image generation).
# Set one or both keys.
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional: when both keys are set, which provider to use by default.
# Values: gemini | openai
DEFAULT_PROVIDER=gemini
